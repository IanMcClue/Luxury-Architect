---
import CheapButton from "../utils/CheapButton.astro";
import ContactInput from "../utils/ContactInput.astro";
---

<section class="mb-20">
	<h2 class="text-title mb-8">Enquire about one of our luxury holiday homes</h2>
	<form class="grid gap-4 md:grid-cols-2 md:gap-8">
		<ContactInput id="1" label="name" placeholder="input your name" />
		<ContactInput id="2" label="mobile number" placeholder="input your mobile number" />
		<ContactInput id="2" type="email" label="email" placeholder="input your email" />
        <div>
            <span class="block mb-2 opacity-80">services</span>
            <div class="flex items-end gap-4 flex-wrap text-sm">
                <CheapButton>Book A Date</CheapButton>
                <CheapButton>Sign In</CheapButton>
                <CheapButton>FAQ</CheapButton>
            </div>
        </div>
	</form>
</section>
